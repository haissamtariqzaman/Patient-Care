<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.patientcare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PatientCare">
        <activity android:name=".AppointmentDetail"></activity>
        <activity android:name=".AppointmentActivity" />
        <activity
            android:name=".DoctorHomeScreen"
            android:label="Doctor&apos;s Home" />
        <activity android:name=".RecyclerView_Doctors" />
        <activity android:name=".FindDoctor" />
        <activity android:name=".PatientHomeScreen" />
        <activity android:name=".PatientSignup" />
        <activity android:name=".DoctorSignup" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>